import { Menu } from "@superapp/icons";
import { IconButton } from "@superapp/ui";

const Home = () => {
  return (
    <div>
      <div>
        <IconButton>
          <Menu />
        </IconButton>
      </div>
    </div>
  );
};

export default Home;
